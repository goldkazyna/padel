# Групповые занятия — чек-лист проверки (прод)

Проверять сверху вниз. После каждого шага — ожидаемый результат.

## 0. Подготовка ✅
- [x] Деплой выполнен (5 миграций через `--path` + `php artisan optimize:clear`).
- [x] Зайти под **club_admin** клуба, где есть ≥1 корт и пара CRM-клиентов. Тренер — по желанию.

## 1. Фича-флаг и навигация ✅
- [x] В сайдбаре есть **«Группы»** и **«Журнал занятий»**.
- [x] (Супер-админ) в `/admin/clubs/{id}/edit` → «Доступные модули» есть чекбокс **«Групповые занятия»** (включён).

## 2. Создание группы ✅
- [x] «Группы» → «Создать группу»: имя, тренер, цена за занятие, вместимость = 2, заметка → сохранить.
- [x] Группа в списке, карточка открывается.
- [x] «Редактировать» — меняется имя/цена/статус.

## 3. Добавление участника с пакетом ✅
- [x] «Добавить участника»: клиент (динамический поиск по имени/телефону), кол-во занятий = 8, сумма, «оплачено» → добавить.
- [x] Остаток у участника = **8**.
- [x] Добавить того же клиента ещё раз → ошибка «уже в этой группе».

## 4. Продление пакета
- [ ] «Продлить»: +4 → остаток = **12**.

## 5. Вместимость
- [ ] Добавить второго клиента (2/2).
- [ ] Добавить третьего → ошибка «Группа заполнена». Не добавился.

## 6. Создание занятия (резерв корта)
- [ ] «Журнал занятий» → «Создать занятие»: группа, корт, дата (завтра), время, slots, тренер → создать.
- [ ] Занятие в журнале, статус «запланировано».
- [ ] В **расписании кортов** на эту дату/время слот **занят**, помечен как групповой.

## 7. Конфликт корта
- [ ] Создать ещё занятие на тот же корт/дату/время → ошибка «Корт занят». Не создалось.

## 8. Проведение + посещаемость + списание
- [ ] Открыть занятие → ростер с остатками.
- [ ] Отметить одного «пришёл» + «списать», другого «не пришёл» → «Провести занятие».
- [ ] Статус «проведено».
- [ ] У пришедшего остаток −1; у непришедшего без изменений.

## 9. Блок при нулевом остатке
- [ ] Участник с остатком 0 → отметить «пришёл» + «списать» → ошибка «закончились занятия — продлите пакет», занятие НЕ проводится.
- [ ] Продлить пакет → проводится.

## 10. Повторное проведение запрещено
- [ ] Проведённое занятие → формы отметки нет (только просмотр).
- [ ] Повторно провести → ошибка «Занятие уже проведено», остаток не меняется.

## 11. Отмена занятия
- [ ] Запланированное занятие → «Отменить занятие».
- [ ] Статус «отменено», в расписании слот **освободился**, списаний нет.

## 12. Карточка клиента
- [ ] `/club/clients` → выбрать клиента из группы → блок **«Группы»** с названием и **остатком**, ссылка ведёт в группу.

## 13. Доступ модератора / выключенный модуль
- [ ] Под **модератором** разделы «Группы»/«Журнал» работают.
- [ ] (Супер-админ) выключить фичу у клуба → пункты пропали, прямой заход на `/club/groups` → 403.

---

Ключевые интеграции: **шаг 6 (слот в расписании)**, **шаг 8 (списание)**, **шаг 11 (освобождение корта)**.
