php artisan tinker --execute='$user = \App\Models\User::find(175); echo "User: ".$user->name." | Текущий рейтинг: ".$user->rating.PHP_EOL.PHP_EOL; $rows = \App\Models\RatingHistory::where("user_id", 175)->orderBy("created_at")->get(); foreach($rows as $r) { echo $r->created_at." | ".$r->reason." | ".$r->rating_before." → ".$r->rating_after." (".($r->change > 0 ? "+" : "").$r->change.") | t_id=".$r->tournament_id.PHP_EOL; } echo PHP_EOL."Всего записей: ".$rows->count().PHP_EOL;'