php artisan tinker --execute='$title = "Обновление приложения"; $body = "Вышла новая версия Padel KZ! Обновите приложение для стабильной работы и новых функций."; $users = \App\Models\User::whereHas("deviceTokens")->get(); $service = app(\App\Services\FCMNotificationService::class); $count = 0; foreach ($users as $user) { \App\Models\Notification::create(["user_id" => $user->id, "title" => $title, "body" => $body, "type" => "update"]); $service->sendToUser($user, $title, $body, ["type" => "update"]); $count++; } echo "Отправлено: $count пользователям\n";'
